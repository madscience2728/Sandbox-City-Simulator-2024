using System.Numerics;
using Network.Core;

namespace Sandbox_City_Simulator_2024;

public class AbstractGameHost : Host
{    
    public AbstractGameHost(string name, string gateway) : base(name, gateway)
    {
 
    }
}