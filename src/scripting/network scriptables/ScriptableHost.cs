namespace Sandbox_Simulator_2024;

using Network.Core;

public class ScriptableHost : Host
{
    public ScriptableHost(string name, string defaultGateway) : base(name, defaultGateway)
    {
        
    }
}